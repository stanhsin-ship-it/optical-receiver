# QR 光學傳檔：接收端與圖形化發送端

本指南只說明兩個日常使用的工具：

- `qrgrab.py`：在接收端擷取螢幕上的 QR Code、還原檔案。
- `qrsendgui.py`：在發送端用圖形介面選檔並播放 QR Code。

適合無網路、隔離環境，或透過 NoMachine 等遠端桌面傳檔。

## 先準備接收端：`qrgrab.py`

在**看得到 QR Code 畫面的電腦**安裝 Python 套件：

```bash
python3 -m pip install opencv-python-headless mss numpy pyzbar
```

建議安裝 ZBar，讓 QR 解碼在低畫質或遠端桌面情境下更穩定：

```bash
# Ubuntu / Debian
sudo apt install libzbar0
```

啟動接收器。以下範例擷取第 2 個螢幕，完成的檔案會存到 `./received/`：

```bash
python3 qrgrab.py --monitor 2
```

若不知道螢幕編號，可先執行：

```bash
python3 -c "from mss import mss; [print(i, m) for i, m in enumerate(mss().monitors)]"
```

輸出中的 `1`、`2`、`3` 等就是可傳給 `--monitor` 的編號。

## 用圖形介面發送：`qrsendgui.py`

在**存放原始檔案的電腦**執行：

```bash
python3 qrsendgui.py
```

它需要 Python 的 `tkinter`。QR 產生套件 `segno` 若未安裝，程式會優先使用專案中附帶的 wheel；仍無法啟動時再安裝：

```bash
python3 -m pip install segno
```

操作步驟：

1. 在左側檔案瀏覽器選擇一個或多個檔案，按 **Send selected**；要傳整個資料夾則按 **Send this whole folder**。
2. 在右側確認設定。第一次可維持預設值。
3. 先按 **Analyze**，查看畫格數量與預估播放時間。
4. 按 **Send** 開啟 QR 播放視窗。請讓接收端所選螢幕能完整看見該視窗。
5. 接收端出現 `Checksum verified` 時，表示檔案已完整還原。

一次選多個檔案時，程式會依序播放，接收器會將它們分別存成獨立檔案。

## 發送設定建議

| 設定 | 建議 |
| --- | --- |
| **QR version** | 預設即可；版本越高，每張 QR 可放更多資料，但碼也更密、越難解碼。 |
| **Frames / second** | 預設可先嘗試；NoMachine 畫面模糊或掉幀時，調低到 `1`。 |
| **Passes** | `3–5` 通常足夠；`0` 會持續循環，直到手動停止。 |
| **Split size** | 大檔案會分段傳送，通常保留預設值。 |
| **Fullscreen sender window** | 遠端桌面或螢幕較小時建議勾選，可用 F11 切換。 |

播放視窗中：空白鍵可暫停／繼續；暫停後可用方向鍵逐張檢查；Esc 或 `q` 停止播放。

## 接收端常用選項

```bash
# 改為每秒擷取 8 次，並指定輸出資料夾
python3 qrgrab.py --monitor 2 --fps 8 --out ./incoming

# 只接收一個檔案後結束
python3 qrgrab.py --monitor 2 --once

# 資料夾傳送後保留 .tar，不自動解開
python3 qrgrab.py --monitor 2 --no-untar
```

## 缺少畫格時怎麼辦

若接收端顯示 `missing: 7,27,60-62`，請在 `qrsendgui.py` 的 **Replay only** 欄位貼上該行（也可以直接貼接收端印出的整段 `--only ...` 指令）。接著按 **Send**，就會只重播缺少的畫格。

重播必須選擇與原始傳送相同的檔案，並保持原本的 **QR version** 設定。

## 注意事項

- QR Code 必須完整顯示在接收端擷取的螢幕範圍內，避免被視窗、通知或滑鼠游標遮住。
- 接收器完成一個檔案後會繼續等待下一個傳送；按 Ctrl+C 才停止。
- 工具會以 SHA-256 驗證檔案完整性，但不會加密內容、也不驗證發送者身分。僅應在可信任環境中傳送機密資料。
