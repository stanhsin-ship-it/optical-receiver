# 📸 光學傳檔小工具 使用指南

> 用「螢幕閃 QR code、另一端用眼睛看」的方式，把檔案從沒有網路的機器搬出來。
> 沒有網路線、沒有隨身碟、沒有雲端，只有光。✨

---

## ⚡ 一頁速查表（老手直接抄）

```bash
# ── 送檔端（沒網路的 Linux，用 NoMachine 看著它）────────────
python3 qrsendbin.py 檔案            # 一直播，Esc 手動停
python3 qrsendbin.py 檔案 --passes 3 # 播 3 輪自動停 ⭐
python3 qrsendbin.py 檔案 --fps 1    # NoMachine 壓縮嚴重時放慢
python3 qrsendbin.py 檔案 --only 7,27,60-62   # 只補這幾格
python3 qrsendbin.py 資料夾/ --passes 3  # 傳整個資料夾（自動打包成 .tar）📁

# ── 收檔端（你的電腦，抓螢幕）⭐推薦 ─────────────────────
python3 qrgrab.py --monitor 2       # 抓第二螢幕，存到 ./received

# ── 收檔端（手機）──────────────────────────────────────
# 把 qrrecvbin.html 放上 GitHub Pages → 開一次 → 加入主畫面 → 用鏡頭對螢幕

# ── 大檔案：不用管，自動切、自動拼 ⭐ ─────────────────────
# 照平常一樣傳就好，送檔端會自動切、qrgrab 會自動拼回並驗 checksum
python3 qrsendbin.py 大檔案 --passes 3    # 大檔自動切成多份播出
# （只有用「手機」收多份時才要手動拼：）
python3 qrjoin.py received/ --out restored/
```

| 出現這個 | 代表 | 怎麼辦 |
|----------|------|--------|
| `(decoder: pyzbar)` | 一切正常 ✅ | 放心用 |
| `(decoder: opencv)` | ZBar 沒裝好 ⚠️ | 裝 `libzbar0`／VC++ 2013 |
| `Checksum verified.` | 成功！🎉 | 收工 |
| `missing: 7,27,60-62` | 缺這幾格 | 用 `--only` 補 |

> 標準流程：**先開收檔端 → 再開送檔端 `--passes 3` → 等收檔端印出 `Done`。**

---

## 🤔 這是什麼？為什麼要這樣搞？

我們有一台 Linux 機器**沒有對外網路**，只能透過 NoMachine 遠端看它的畫面。
要把上面的小檔案（文字、設定檔、小 xlsx…）弄出來，傳統方法都行不通。

於是我們讓它**把檔案切成一張張 QR code，在螢幕上輪流播放**，
接收端只要「看著螢幕」就能把檔案一格一格拼回來。

```
  沒網路的 Linux                      你的電腦 / 手機
  ┌──────────────┐                  ┌──────────────┐
  │   qrsend     │   QR 一直閃 →     │   接收端      │
  │  把檔案變成   │  ～～～～～～～→   │  看螢幕、拼檔  │
  │  一堆 QR code │   （只有光）      │  對完就存檔    │
  └──────────────┘                  └──────────────┘
```

重點：**資料只會往一個方向跑**，接收端無法反向送任何東西回去，
所以那台機器的隔離狀態完全不受影響。🔒

---

## 🧰 工具箱有什麼

| 檔案 | 角色 | 跑在哪 |
|------|------|--------|
| `qrsend.py` | 送出**純文字**檔 | 沒網路的 Linux（用 NoMachine 看） |
| `qrsendbin.py` | 送出**任何檔案**（含二進位、xlsx…） | 同上 |
| `qrgrab.py` | 接收：**直接抓螢幕**來解碼 ⭐推薦 | 你的 NoMachine 用戶端電腦 |
| `qrrecv.html` | 接收：**手機瀏覽器**（純文字版） | 手機 |
| `qrrecvbin.html` | 接收：**手機瀏覽器**（任何檔案） | 手機 |
| `qrsplit.py` | 大檔案：**切成很多小份** | 沒網路的 Linux |
| `qrjoin.py` | 大檔案：**把小份拼回原檔** | 收檔端電腦 |

> 兩個送檔程式、三個收檔程式，外加一對大檔案切／拼工具，可以自由搭配。
> 收檔端會自動辨識是哪一種送檔，不用你手動選。👍

---

## 🚀 最快上手（推薦組合）

**送檔端（Linux，用 NoMachine 看著它）：**

```bash
python3 qrsendbin.py 你的檔案.xlsx --passes 3
```

**收檔端（你的電腦）：**

```bash
python3 qrgrab.py --monitor 2
```

然後把 NoMachine 視窗放到**第二個螢幕**、放到最大，
盯著看幾秒鐘，收檔端印出這行就完成了：

```
Done. Wrote ./received/你的檔案.xlsx  (14964 bytes). Checksum verified. ✅
```

就這樣！檔案已經帶著正確的 checksum 安全落地了。🎉

---

## 🛠️ 第一次安裝（只需一次）

### 送檔端（Linux）
只需要一個套件：
```bash
pip install segno
```

### 收檔端（用 `qrgrab.py` 的那台電腦）
```bash
pip install opencv-python-headless mss numpy pyzbar
```

還要裝 **ZBar** 這個系統函式庫（`pyzbar` 靠它解碼，少了它會變慢又容易漏格）：

| 系統 | 指令 |
|------|------|
| Ubuntu / Debian | `sudo apt install libzbar0` |
| macOS | `brew install zbar` |
| Windows | pip 裝好通常就內含了，但若報錯見下方 🐛 |

> ✅ **怎麼確認裝好了？** 跑 `qrgrab.py` 時，開頭那行要寫 `(decoder: pyzbar)`。
> 如果寫的是 `(decoder: opencv)`，代表 ZBar 沒載入成功 —— 會有些格子**永遠**解不出來。

---

## 📖 各程式怎麼用

### 送檔：`qrsendbin.py`（萬用，最常用）

```bash
python3 qrsendbin.py 檔案或資料夾 [選項]
```

> 給**檔案**就傳那個檔；給**資料夾**就自動打包成 `.tar` 整包傳（見下方 📁 章節）。

| 選項 | 意思 | 建議 |
|------|------|------|
| `--passes N` | 播 N 輪後**自動停**並關掉視窗 | 設 `3`～`5`，最省事 |
| `--fps 1` | 每秒幾張（越慢越穩） | NoMachine 壓縮嚴重時設 `1` |
| `--version 6` | QR 密度（越小越好認、但格數變多） | 預設 `6` 就很穩 |
| `--only 7,19,55-61` | 只重播指定的格子 | 補漏用，見下 👇 |

### 收檔：`qrgrab.py`（抓螢幕，推薦）

```bash
python3 qrgrab.py --monitor 2
```

| 選項 | 意思 |
|------|------|
| `--monitor 2` | 抓第幾號螢幕（把 NoMachine 放這個螢幕） |
| `--fps 8` | 每秒抓幾次（預設 15，CPU 太忙可調小） |
| `--out ./資料夾` | 存檔位置（預設 `./received`） |

> 不確定第二螢幕是幾號？跑這行看看：
> ```bash
> python3 -c "from mss import mss; [print(i, m) for i, m in enumerate(mss().monitors)]"
> ```
> `0` 是「全部螢幕合起來」別選它，`1`、`2` 才是各別螢幕。

### 收檔：手機版 `qrrecvbin.html`

放到 **GitHub Pages**（或任何 https 網站）開一次，
然後「加入主畫面」，之後就能離線用。
用手機鏡頭對著 NoMachine 畫面即可。📱

> 為什麼要放網站不能直接開檔？因為 iPhone 只有在 `https://` 下才給用相機。
> 這頁本身**不含任何機密**，只是個解碼器，放公開網站沒問題。

---

## 🩹 漏了幾格怎麼辦？（超好用）

檔案大時偶爾會有幾格一直沒收到。收檔端會告訴你**缺哪幾格**：

```
[####################........]  55/63   missing: 7,27,60-62
```

手機版可以按 **Copy missing** 複製；`qrgrab.py` 按 Ctrl-C 也會印出來。
然後回送檔端，只重播那幾格就好，不用整個檔重來：

```bash
python3 qrsendbin.py 檔案 --only 7,27,60-62
```

> ⚠️ 重播時 `--version` 要跟原本那次**一模一樣**，格子邊界才對得上。
> 好消息是 session 編號是從檔案內容算出來的，所以收檔端會自動接續、把洞補起來。😎

---

## 📁 一次傳整個資料夾

不用一個一個檔案傳 —— 直接把**資料夾名稱**丟給送檔端就好：

```bash
python3 qrsendbin.py 我的資料夾/ --passes 3
```

它會自動把整個資料夾打包成一個 `我的資料夾.tar`（**檔名、子資料夾結構都會保留**），
當成一個檔案傳出去。收檔端會收到一個 `.tar`，在那邊解開就好：

```bash
tar xf 我的資料夾.tar
```

> `tar` 在 Windows 10/11、macOS、Linux 都內建，不用另外裝。送檔端也會幫你印出這行提醒。

**兩個貼心設計：**
- **同一個資料夾內容 → 每次都是同一個 session 編號**，所以補傳、`--only` 補格都對得上。
- **資料夾太大也沒問題**：`qrsplit.py` 一樣可以吃資料夾，先打包再切份，最後 `qrjoin.py` 拼回 `.tar`。

> ⚠️ 提醒：打包資料夾只是**操作變方便**，不會變小。
> 十個檔案打包起來，總張數還是那麼多 —— 內容大就還是得走切份流程。

---

## 📦 大檔案怎麼辦？（自動切、自動拼 ⭐）

光學傳檔天生適合**小檔**。檔案一大，一個 session 的 QR 張數會爆炸：

| 檔案大小（v6 預設） | 大約張數 | 一輪大約要 |
|------|------|------|
| 5 KB | ~110 張 | 1 分鐘 |
| 100 KB | ~2,200 張 | 18 分鐘 |
| 1 MB | ~22,000 張 | **3 小時** 😱 |

**好消息：大檔案現在會自動處理，你什麼都不用做。** 照平常一樣傳就好：

```bash
python3 qrsendbin.py 大檔案.bin --passes 3      # 電腦收檔端（qrgrab.py）
```

- **送檔端會自動判斷**：檔案小 → 一個 session 傳完；檔案大 → 自動切成「manifest ＋ 很多份」，一份接一份播出去。你不用加任何參數。
- **收檔端（`qrgrab.py`）會自動辨識**：看到是多份傳輸，就一份一份收集，全部到齊後**自動拼起來、驗整檔 checksum、寫出原檔**，印出 `Rebuilt … Checksum verified`。✅
- 資料夾也一樣：打包成 `.tar` → 太大就自動切 → 自動拼回 `.tar` → `tar xf` 解開。

> 切的門檻是看**壓縮後的張數**（不是原始大小），所以很好壓的文字資料夾常常一個 session 就夠、不會亂切。
> 想調整可用 `--split-size`（預設每份約 40k）或 `--no-split`（不管多大都不切）。

### 手機收檔要手動拼

自動拼只在**電腦收檔端 `qrgrab.py`** 裡。用**手機**收多份時，手機會把每一份存成獨立檔案、**不會自動拼**，這時候用這對手動工具收尾：

```bash
python3 qrsplit.py 大檔案.bin --size 40k --out parts/   # （可選）自己先切
python3 qrjoin.py received/ --out restored/             # 手機收到的份，手動拼回
```

`qrjoin.py` 會檢查每一份都到齊、依序接起來、再驗一次整檔 checksum；**少了哪一份會直接告訴你編號**，只補那一份就好。

> 老實說：切檔只是讓大檔**變得「做得到」，不是「變快」**。
> 真的要常常傳 MB 等級的檔，光學不是對的工具 —— 但沒有別的路時，這招能救命。

---

## 🐛 疑難雜症

**Q：開頭顯示 `(decoder: opencv)`，而且有些格子永遠收不到。**
→ ZBar 沒載入。裝上面說的 `libzbar0` / `zbar`。
→ **Windows 特別注意**：若 `import pyzbar.pyzbar` 報 `FileNotFoundError ... libzbar-64.dll`，
　 是缺 **Visual C++ 2013 可轉散發套件**（要 2013 那版，裝 `vcredist_x64.exe`）。裝完就好。

**Q：抓螢幕比手機還不穩，同樣幾格一直失敗。**
→ 幾乎都是上面那個 decoder 問題。先確認開頭是 `pyzbar`。

**Q：畫面在第二螢幕，但好像更新很慢。**
→ NoMachine 會把**沒被選取的視窗**降速更新。點一下 NoMachine 視窗讓它取得焦點，
　 或把送檔端調成 `--fps 1`，讓每張 QR 停久一點。

**Q：`qrgrab.py` 抓到全黑畫面。**
→ 若是 Linux 上跑收檔端且用 **Wayland**，螢幕擷取可能被系統擋。改用 X11 較穩。

**Q：怎麼知道檔案真的對？**
→ 收檔端會自己驗 **SHA-256**，印出 `Checksum verified.` 才算數。
　 沒印出來就是沒成功，重跑一次即可。

---

## 💡 小提醒

- **速度不是重點，穩定才是。** 送檔端寧可 `--fps 1` 慢慢來。
- **檔案越小越好。** 純文字幾 KB 最理想；大檔請先壓縮或切小。
- **收檔端是唯一的真相來源** —— 它說 `Checksum verified.` 才是真的成功。
- 送檔端 `--passes` 是「定時停」不是「收到才停」（因為我們刻意不開反向通道）。
　 萬一它先停了、檔案還沒到齊，再跑一次 `--passes 3` 就會自動接續補齊。

---

祝傳檔順利，享受這道只用「光」的魔法～ 🪄✨
```

有問題或想加功能，隨時找我們！
