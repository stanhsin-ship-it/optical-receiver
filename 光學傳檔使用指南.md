# 📸 光學傳檔小工具 使用指南

> 用「螢幕閃 QR code、另一端用眼睛看」的方式，把檔案從沒有網路的機器搬出來。
> 沒有網路線、沒有隨身碟、沒有雲端，只有光。✨

---

## 🤔 這是什麼？為什麼要這樣搞？

我們有一台 Linux 機器**沒有對外網路**，只能透過 NoMachine 遠端看它的畫面。
要把上面的小檔案（文字、設定檔、小 xlsx…）弄出來，傳統方法都行不通。

於是我們讓它**把檔案切成一張張 QR code，在螢幕上輪流播放**，
接收端只要「看著螢幕」就能把檔案一格一格拼回來。

```
  沒網路的 Linux                      你的電腦 / 手機
  ┌──────────────┐                  ┌──────────────┐
  │   qrsend     │   QR 一直閃 →     │   接收端      │
  │  把檔案變成   │  ～～～～～～～→   │  看螢幕、拼檔  │
  │  一堆 QR code │   （只有光）      │  對完就存檔    │
  └──────────────┘                  └──────────────┘
```

重點：**資料只會往一個方向跑**，接收端無法反向送任何東西回去，
所以那台機器的隔離狀態完全不受影響。🔒

---

## 🧰 工具箱有什麼

| 檔案 | 角色 | 跑在哪 |
|------|------|--------|
| `qrsend.py` | 送出**純文字**檔 | 沒網路的 Linux（用 NoMachine 看） |
| `qrsendbin.py` | 送出**任何檔案**（含二進位、xlsx…） | 同上 |
| `qrgrab.py` | 接收：**直接抓螢幕**來解碼 ⭐推薦 | 你的 NoMachine 用戶端電腦 |
| `qrrecv.html` | 接收：**手機瀏覽器**（純文字版） | 手機 |
| `qrrecvbin.html` | 接收：**手機瀏覽器**（任何檔案） | 手機 |

> 兩個送檔程式、三個收檔程式，可以自由搭配。
> 收檔端會自動辨識是哪一種送檔，不用你手動選。👍

---

## 🚀 最快上手（推薦組合）

**送檔端（Linux，用 NoMachine 看著它）：**

```bash
python3 qrsendbin.py 你的檔案.xlsx --passes 3
```

**收檔端（你的電腦）：**

```bash
python3 qrgrab.py --monitor 2
```

然後把 NoMachine 視窗放到**第二個螢幕**、放到最大，
盯著看幾秒鐘，收檔端印出這行就完成了：

```
Done. Wrote ./received/你的檔案.xlsx  (14964 bytes). Checksum verified. ✅
```

就這樣！檔案已經帶著正確的 checksum 安全落地了。🎉

---

## 🛠️ 第一次安裝（只需一次）

### 送檔端（Linux）
只需要一個套件：
```bash
pip install segno
```

### 收檔端（用 `qrgrab.py` 的那台電腦）
```bash
pip install opencv-python-headless mss numpy pyzbar
```

還要裝 **ZBar** 這個系統函式庫（`pyzbar` 靠它解碼，少了它會變慢又容易漏格）：

| 系統 | 指令 |
|------|------|
| Ubuntu / Debian | `sudo apt install libzbar0` |
| macOS | `brew install zbar` |
| Windows | pip 裝好通常就內含了，但若報錯見下方 🐛 |

> ✅ **怎麼確認裝好了？** 跑 `qrgrab.py` 時，開頭那行要寫 `(decoder: pyzbar)`。
> 如果寫的是 `(decoder: opencv)`，代表 ZBar 沒載入成功 —— 會有些格子**永遠**解不出來。

---

## 📖 各程式怎麼用

### 送檔：`qrsendbin.py`（萬用，最常用）

```bash
python3 qrsendbin.py 檔案 [選項]
```

| 選項 | 意思 | 建議 |
|------|------|------|
| `--passes N` | 播 N 輪後**自動停**並關掉視窗 | 設 `3`～`5`，最省事 |
| `--fps 1` | 每秒幾張（越慢越穩） | NoMachine 壓縮嚴重時設 `1` |
| `--version 6` | QR 密度（越小越好認、但格數變多） | 預設 `6` 就很穩 |
| `--only 7,19,55-61` | 只重播指定的格子 | 補漏用，見下 👇 |

### 收檔：`qrgrab.py`（抓螢幕，推薦）

```bash
python3 qrgrab.py --monitor 2
```

| 選項 | 意思 |
|------|------|
| `--monitor 2` | 抓第幾號螢幕（把 NoMachine 放這個螢幕） |
| `--fps 8` | 每秒抓幾次（預設 15，CPU 太忙可調小） |
| `--out ./資料夾` | 存檔位置（預設 `./received`） |

> 不確定第二螢幕是幾號？跑這行看看：
> ```bash
> python3 -c "from mss import mss; [print(i, m) for i, m in enumerate(mss().monitors)]"
> ```
> `0` 是「全部螢幕合起來」別選它，`1`、`2` 才是各別螢幕。

### 收檔：手機版 `qrrecvbin.html`

放到 **GitHub Pages**（或任何 https 網站）開一次，
然後「加入主畫面」，之後就能離線用。
用手機鏡頭對著 NoMachine 畫面即可。📱

> 為什麼要放網站不能直接開檔？因為 iPhone 只有在 `https://` 下才給用相機。
> 這頁本身**不含任何機密**，只是個解碼器，放公開網站沒問題。

---

## 🩹 漏了幾格怎麼辦？（超好用）

檔案大時偶爾會有幾格一直沒收到。收檔端會告訴你**缺哪幾格**：

```
[####################........]  55/63   missing: 7,27,60-62
```

手機版可以按 **Copy missing** 複製；`qrgrab.py` 按 Ctrl-C 也會印出來。
然後回送檔端，只重播那幾格就好，不用整個檔重來：

```bash
python3 qrsendbin.py 檔案 --only 7,27,60-62
```

> ⚠️ 重播時 `--version` 要跟原本那次**一模一樣**，格子邊界才對得上。
> 好消息是 session 編號是從檔案內容算出來的，所以收檔端會自動接續、把洞補起來。😎

---

## 🐛 疑難雜症

**Q：開頭顯示 `(decoder: opencv)`，而且有些格子永遠收不到。**
→ ZBar 沒載入。裝上面說的 `libzbar0` / `zbar`。
→ **Windows 特別注意**：若 `import pyzbar.pyzbar` 報 `FileNotFoundError ... libzbar-64.dll`，
　 是缺 **Visual C++ 2013 可轉散發套件**（要 2013 那版，裝 `vcredist_x64.exe`）。裝完就好。

**Q：抓螢幕比手機還不穩，同樣幾格一直失敗。**
→ 幾乎都是上面那個 decoder 問題。先確認開頭是 `pyzbar`。

**Q：畫面在第二螢幕，但好像更新很慢。**
→ NoMachine 會把**沒被選取的視窗**降速更新。點一下 NoMachine 視窗讓它取得焦點，
　 或把送檔端調成 `--fps 1`，讓每張 QR 停久一點。

**Q：`qrgrab.py` 抓到全黑畫面。**
→ 若是 Linux 上跑收檔端且用 **Wayland**，螢幕擷取可能被系統擋。改用 X11 較穩。

**Q：怎麼知道檔案真的對？**
→ 收檔端會自己驗 **SHA-256**，印出 `Checksum verified.` 才算數。
　 沒印出來就是沒成功，重跑一次即可。

---

## 💡 小提醒

- **速度不是重點，穩定才是。** 送檔端寧可 `--fps 1` 慢慢來。
- **檔案越小越好。** 純文字幾 KB 最理想；大檔請先壓縮或切小。
- **收檔端是唯一的真相來源** —— 它說 `Checksum verified.` 才是真的成功。
- 送檔端 `--passes` 是「定時停」不是「收到才停」（因為我們刻意不開反向通道）。
　 萬一它先停了、檔案還沒到齊，再跑一次 `--passes 3` 就會自動接續補齊。

---

祝傳檔順利，享受這道只用「光」的魔法～ 🪄✨
```

有問題或想加功能，隨時找我們！
